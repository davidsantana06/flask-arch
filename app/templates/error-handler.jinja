{% extends layout('base') %}

{% block title %}{{ error.code }}{% endblock %}

{% block style %}
    <link rel="stylesheet" href="{{ css('error-handler') }}">
{% endblock %}

{% block content %}
    <main class="container d-flex align-items-center justify-content-center py-min">
        <div id="error" class="shadow-lg">
            <div class="bg-white px-3 py-1">
                <span id="code" class="fw-semibold">_{{ error.code }}</span>
            </div>
            <div class="bg-dark p-3">
                <span id="description" class="fw-light">{{ error.description }}</span>
            </div>
        </div>
    </main>
{% endblock %}